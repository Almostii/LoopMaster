# 参与开发

感谢你关注 LoopMaster！本文档说明如何在本仓库中提交问题反馈与代码贡献。

> 注：本仓库为公开分发的源码仓库。更完整的内部设计文档与开发日志保存在私有仓库，不随公开仓库分发；本文档已包含对外贡献所需的全部约定。

## 行为准则

- 友好、尊重、就事论事；不人身攻击，不提交与项目无关的内容。
- 公开 Issue 或讨论时，请勿包含真实账号、密钥、token、个人路径、设备私有配置、运行日志或音频内容。

## 如何反馈问题

- **Bug 报告**：说明复现步骤、预期行为、实际行为、Windows 版本、音频设备与驱动、以及是否使用了虚拟声卡（如 VB-CABLE）。如涉及音频链路，附上界面截图与 `engine-stats` 信息更有帮助。
- **功能建议**：先确认它是否属于 LoopMaster 的产品边界（可视化音频路由与回环监听，**不包含**虚拟声卡创建、Pass-Thru 透传、新建虚拟设备等系统级设备管理能力）。

## 开发环境

见根目录 [`README.md`](README.md#从源码构建) 的「从源码构建」。要点：

- Windows 10 / 11（x64）；
- Rust stable（[rustup](https://rustup.rs/)）；
- Node.js 18+（含 npm）；
- C++ 生成工具（Windows SDK）；
- WebView2 运行时。

## 分支约定

- `main`：唯一的稳定基线与默认发布分支，**禁止直接提交、直接推送或直接修改工作树后提交**。
- 所有开发、调试、文档、测试与配置修改，都必须在专用分支中进行：
  - `feature/<topic>`：新功能；
  - `fix/<topic>`：缺陷修复；
  - `docs/<topic>`：文档；
  - `chore/<topic>`：构建/配置等非功能改动；
  - `spike/<topic>`：短期技术验证（结束后合并结论或删除分支）。
- 分支名使用小写英文、数字和连字符，主题应说明工作内容，不使用 `test`、`new`、`临时` 等无信息名称。
- 工作开始前从最新的目标基线创建分支；完成后通过审查与验证再合并回 `main`。

## 提交规范

- 一次提交只解决一个逻辑问题，避免把格式化、重命名和功能修改混在一起。
- 提交信息格式：`<type>(<scope>): <summary>`。
  - `type` 与 `scope` 保留英文（便于工具识别）；
  - `summary` **必须使用中文**，采用祈使式、简洁描述结果，例如「配置 Windows 安装包打包（NSIS + MSI）」，不写「修改一些问题」。
- 提交前检查工作树，禁止覆盖他人未提交的修改。
- 不提交密钥、账号、个人路径、临时日志、设备私有配置、编译产物或未经许可的第三方代码。

常用 `type`：`feat`（新功能）、`fix`（缺陷）、`docs`（文档）、`refactor`（重构）、`test`（测试）、`chore`（构建/配置）。

## 提交前检查

提交正式代码前至少运行：

```powershell
# Rust workspace 门禁（根目录）
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --workspace

# 前端（frontend/）
cd frontend
npm install
npm run build
```

- 如果涉及真实设备，必须附带测试环境与结果；无法运行某项检查时，提交说明必须明确原因。
- 前端依赖、构建产物、`dist/`、`node_modules/`、安装包 `dist-installer/` 均不提交。

## 合并要求

- 所有改动必须先在专用分支验证，再合并到 `main`；禁止从本地工作树直接提交到 `main`。
- `main` 只接受经过审查的分支合并；合并后应确认目标分支仍能构建和测试通过。
- 禁止为了「让历史干净」而改写共享分支历史；已发布或被他人使用的提交不使用强制推送、`reset --hard` 或删除分支来隐藏问题。
- 需要回退时优先使用新的 `revert` 提交，并在日志中记录影响。
- 无后续工作时删除已合并的短期分支。

## 文档规则

- 本文档、README 等对外文档的变更，应与对应代码在同一功能分支中提交。
- 内部设计文档（`Doc/`，私有仓库）与本地参考资料（`Library/`）不随公开仓库分发；如需参考代码进入正式项目，必须复制到项目源码目录并单独完成审查。

## 许可证

贡献的代码将采用与本项目一致的 [知识共享署名-非商业性使用 4.0 国际许可协议（CC BY-NC 4.0）](LICENSE)。该协议允许在非商业前提下复制、再分发和创作演绎作品，并要求署名，**不得用于商业目的**。
