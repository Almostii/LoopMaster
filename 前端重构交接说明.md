# LoopMaster 前端重构交接说明

更新时间：2026-08-25

本文是给下一位 AI coding agent 的前端重构执行入口。开始前必须阅读本文、根目录的 `AI交接开发说明.md`、`Doc/Main/1-9`，以及 `Doc/Log/` 最近的开发记录。本文只描述当前已经确认的架构事实和下一阶段任务，不把旧 Slint 原型的能力当成新前端现状。

## 1. 当前基线

项目是 Windows 用户态音频路由器。它不创建或签名虚拟音频驱动，只把捕获的声音经过 LoopMaster 内部混音和路由后写入已经存在的物理设备或 VB-CABLE 等虚拟 endpoint。

当前正式技术栈：

- 音频内核：Rust `audio-core`；
- Windows 适配：Rust `audio-windows`，使用 WASAPI 和 `windows` crate；
- 应用服务：Rust `app-service`，是前端唯一业务服务边界；
- 诊断工具：Rust `diagnostics`；
- 正式前端：`Tauri 2 + React + TypeScript`；
- 旧 Slint 前端：只保存在 `Library/LoopMaster-Slint-archive-2026-08-25/`，不属于 Git，不得重新加入 workspace。

迁移准备分支已经完成并合并到 `main`。Rust workspace 已通过：

```text
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo check --workspace
cargo test --workspace
cargo check --workspace --target x86_64-pc-windows-msvc
git diff --check
```

当前 `frontend/` 已完成 Tauri 2 + React + TypeScript 壳层初始化（分支 `codex/feature-tauri-init`，2026-08-25）：最小窗口可启动，`frontend/src-tauri` 通过路径依赖使用 `app-service`，并提供 `list_devices` 作为最小验证接口。完整的 command/event 适配层、路由编辑和主路由工作区尚未实现，属于下一阶段任务。

## 2. 不可违反的边界

React 只负责页面、交互、展示模型和用户意图。React 不得：

- 直接依赖 `audio-windows`、WASAPI、COM、FIFO 或 `AudioEngine`；
- 在浏览器/React 线程枚举设备或进程；
- 直接读取实时音频线程数据；
- 自己猜测 Running、Degraded、Reconnecting、Failed 等引擎状态；
- 把内部 Bus 宣称为 Windows 虚拟设备。

Tauri command 是前端调用 Rust 应用服务的唯一命令边界，Tauri event 是状态、统计、设备变化的通知边界。`frontend/src-tauri/` 可以依赖 `app-service` 的稳定投影和 DTO，但不应把整个 Windows API 类型树暴露给 TypeScript。

`frontend/src-tauri/` 是 Tauri 自带的 Rust 壳层，不要把它随意加入根 Rust workspace。它可以作为独立 Tauri crate，通过路径依赖使用 `app-service`；是否加入根 workspace 必须先评估 Cargo workspace 边界和构建时间，不能为了方便复制音频引擎代码。

## 3. 当前已实现的 Rust 服务能力

`app-service` 已有：

- 设备模型和设备枚举；
- 有 WASAPI 音频会话的 Process Loopback 来源枚举；
- `RouteEditor`，当前内部实现支持 source、bus、sink、send 的增删和 send 参数编辑；
- `EngineCommand`：Start、Stop、ApplyRoute、SetGain、SetMuted、SetSendEnabled；
- `ServiceEvent`：状态、统计、设备丢失、设备恢复；
- `EngineService::subscribe` 和 `request_reconnect`；
- `AppConfig` v1 的 JSON 校验、稳定 endpoint ID、缺失设备标记和原子保存。

这些 Rust 类型不是前端可以直接序列化的最终协议。Tauri 层要定义稳定、可审查的前端 DTO，保留错误分类、endpoint ID、HRESULT（如有）和中文恢复建议。

## 4. 下一阶段路线

### 阶段 A：初始化 Tauri 工程

从最新 `main` 创建分支：

```powershell
git switch main
git pull
git switch -c codex/feature-tauri-init
```

在 `frontend/` 使用 Tauri 2 官方 React + TypeScript 模板。初始化前检查 Node.js、npm、Rust stable、Tauri CLI、Windows WebView2。不得把旧 `app/` 恢复到 workspace。

本阶段只完成：Tauri 最小窗口、React/TypeScript 构建、开发命令、基础目录、中文编码和最小错误边界。不要在此阶段实现复杂路由页面。

验收：

- `npm install` 和前端开发构建可重复执行；
- `npm run tauri dev` 能启动最小窗口；
- `frontend/src-tauri` 能通过路径依赖访问 `app-service` 的最小测试接口；
- 根目录 `cargo test --workspace` 仍通过；
- `node_modules/`、`dist/`、`.tauri/` 不进入 Git。

### 阶段 B：建立 Tauri command/event 适配层

先实现只读命令和事件闭环，再实现写操作。建议命令分组：

- `list_devices`：返回设备 ID、名称、流向、格式、声道、兼容性和状态；
- `list_audio_processes`：返回当前有音频会话的进程；
- `get_route_snapshot`：返回 Route Profile 视图模型（Sources、Output Channels、External Outputs、Monitors）；内部 bus/sink 仅作为适配层实现细节；
- `start_engine` / `stop_engine` / `request_reconnect`；
- `apply_route_edit`：返回结构化错误和“需要重启”结果；
- 后续再接入 `save_config`、`load_config` 和预设操作。

事件至少覆盖：

- `engine-state-changed`；
- `engine-stats-changed`；
- `device-lost` / `device-restored`；
- 统一的服务错误事件。

所有 WASAPI 枚举和引擎控制都在后台服务线程执行。React 只订阅事件并更新展示状态，不使用高频轮询直接读取 `AudioEngine`。

验收：断开设备时 UI 显示重连/降级，恢复后显示恢复；命令失败时保留错误分类、设备 ID 和建议；拓扑变化明确显示“需要重启”。

### 阶段 C：主路由工作区 MVP

先做一个主页面，不要同时扩展独立设备页、预设页和诊断页。页面语义固定为：

```text
Sources  ->  Output Channels  ->  External Outputs / Monitors
```

必须支持：

- 添加和移除 Source、Output Channel、External Output/Monitor；Output Channel 在当前 Rust 引擎中可由内部 bus 实现，但 UI 不得把 bus 当作设备；
- 创建、删除、启用/禁用、静音和调整每条 send；
- 每条 send 的增益和 channel map 展示；
- 设备 unavailable、unsupported、reconnecting、failed 状态；
- loading、empty、error 状态；
- 刷新音源且不阻塞 UI 或实时音频线程；
- 用户无需看命令行即可完成“应用声音 -> VB-CABLE 播放端 -> 其他应用选择 VB-CABLE 录音端”。

不要保留以下错误语义：`新建虚拟设备`、`Virtual Mic for Zoom`、固定 `Channels 1 & 2`、静态单 source/单 sink 连线。Output Channel、内部 Bus 和 Route Profile 都不是系统设备。

### 阶段 D：真实设备门禁

前端操作闭环后再做实机矩阵：VB-CABLE、物理 USB 声卡、44.1/48 kHz、16-bit PCM、单声道/双声道和已确认的多声道 endpoint，覆盖 Device Loopback、Process Loopback、拔插恢复、30 分钟和 2 小时连续运行。每次测试必须写入 `Doc/Log/YYYY-MM-DD-主题.md`，不能只描述“听起来正常”。

## 5. Git 协作要求

- `main` 只接受经过审查和门禁验证的分支合并；禁止直接提交；
- 每个阶段创建独立 `codex/` 分支；
- 提交 summary 使用中文，例如 `feat(frontend): 初始化 Tauri React 工程`；
- 一个提交只解决一个逻辑问题；
- 合并前运行适用的 Rust、前端构建和 `git diff --check`；
- `Library/` 和 `.workbuddy/` 永远不提交；
- 不要用 reset、checkout 覆盖其他 agent 或用户的修改；
- 完成后更新 `Doc/Main` 和 `Doc/Log`，让代码、文档和验证结果一致。

## 6. 下一位 agent 任务

阶段 A（壳层初始化，`codex/feature-tauri-init`）与阶段 B（command/event 适配层闭环，`codex/feature-tauri-command-event`）已于 2026-08-25 完成并合并。当前进入阶段 C：实现主路由工作区 MVP。

1. 阅读本文、`AI交接开发说明.md`、`Doc/Main/1-9`、`Doc/Log/2026-08-25-tauri-command-event.md` 和最近三份日志；
2. 从最新 `main` 创建新的 `codex/` 分支（不要在已合并的阶段分支上堆叠）；
3. 实现主路由工作区：完整 Sources → Output Channels → External Outputs/Monitors 可视化路由表，每条 send 的启用/静音/增益/channel map；
4. 在主路由工作区展示设备流向、格式、声道、兼容性和缺失状态，以及实时状态、峰值、packet/frame、FIFO、discontinuity、重连次数；
5. 所有界面文本使用中文；旧 Slint 组件只保留在 Library，不作为新前端实现依据；
6. 更新本文档、`frontend/README.md` 和 `Doc/Log`，记录实际命令、版本和未完成项；
7. 通过门禁后再进入阶段 D 真实设备门禁，不要用 UI 开发掩盖未验证的音频问题。
